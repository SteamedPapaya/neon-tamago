server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://mysql:3306/event_management?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
    username: siyoon
    password: 375632
#    url: jdbc:mysql://localhost:3306/event_management
#    username: root
#    password: 375632
#  driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
    hibernate:
      ddl-auto: create
    show-sql: true
  #    defer-datasource-initialization: true
  mvc:
    cors:
      mappings:
        - path: "/**"
          allowed-origins:
            - "https://api.neon7.site"
            - "https://neon7.site"
          allowed-methods: "GET,POST,PUT,DELETE,OPTIONS"
  sql:
    init:
      mode: always
      platform: mysql